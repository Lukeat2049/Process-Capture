{
  "name": "process-capture",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm run --prefix frontend build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": "20.x"
  }
}
