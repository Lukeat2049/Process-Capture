{
  "name": "process-capture",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --include=dev --prefix frontend && npm run --prefix frontend build",
    "start": "node backend/src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.38.5"
  },
  "engines": {
    "node": "20.x"
  }
}
